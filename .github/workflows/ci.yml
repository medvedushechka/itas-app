name: РџСЂРѕРІРµСЂРєР° РїСЂРѕРµРєС‚Р°

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - name: РџРѕР»СѓС‡РёС‚СЊ РєРѕРґ
        uses: actions/checkout@v4

      - name: РќР°СЃС‚СЂРѕРёС‚СЊ Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: РЈСЃС‚Р°РЅРѕРІРёС‚СЊ Р·Р°РІРёСЃРёРјРѕСЃС‚Рё
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: РџСЂРѕРІРµСЂРёС‚СЊ Python-СЃРёРЅС‚Р°РєСЃРёСЃ
        run: python -m compileall -q .

      - name: Р—Р°РїСѓСЃС‚РёС‚СЊ С‚РµСЃС‚С‹
        run: python -m pytest

